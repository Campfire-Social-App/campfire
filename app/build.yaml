# The server speaks snake_case everywhere, so rather than annotating ~60 fields
# one at a time, the rename is set once here and the Dart side stays idiomatic.
targets:
  $default:
    builders:
      json_serializable:
        options:
          field_rename: snake
          explicit_to_json: true
          create_to_json: true
